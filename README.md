# Text-to-SQL
System for e-commerce data. It uses DuckDB for data storage, FastAPI for the backend API, Streamlit for the frontend, and integrate RAGAS for evaluation. It loads e-commerce CSVs into DuckDB, extracting and indexing schema with ChromaDB, using an LLM for SQL generation and implementing a conversational interface.
